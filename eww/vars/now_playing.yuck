;; Now playing poll (uses existing script with emoji/icon prefix).

(defpoll album_art_path
  :interval "2s"
  "sh -lc 'art=\"${XDG_CACHE_HOME:-$HOME/.cache}/hyprlock/album.jpg\"; mtime=$(stat -c %Y \"$art\" 2>/dev/null || echo 0); printf \"%s?%s\\n\" \"$art\" \"$mtime\"'")
