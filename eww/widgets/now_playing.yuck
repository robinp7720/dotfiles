;; Now playing art-only widget.

(defwidget now-playing []
  (image :class "now-art-only"
         :path (string "/home/robin/.cache/hyprlock/album.jpg?" album_mtime)
         :width 120 :height 120 :keep-aspect true))
