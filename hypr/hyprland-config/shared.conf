#
# Main Hyprland session composition.
# Keep this as an include list so greetd can reuse `base.conf` safely.
#

source = ~/.config/hypr/hyprland-config/base.conf
source = ~/.config/hypr/hyprland-config/startup.conf
source = ~/.config/hypr/hyprland-config/bindings.conf
