[module/date]
type = custom/script

interval = 1.0

exec = date "+%Y-%m-%d"

format-background = ${color.background-alt}
format-foreground = ${color.foreground-date}
format-underline = ${color.accent-alt}
format-padding = 4
format-prefix = " "
format-prefix-foreground = ${color.accent-alt}

click-left = alacritty -T Calendar -e "/home/robin/.scripts/calendar.sh"

# vi: ft=dosini
